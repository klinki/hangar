{
  "name": "copilot-session-explorer",
  "version": "0.1.0",
  "private": true,
  "type": "module",
  "scripts": {
    "build": "bun run scripts/build.ts",
    "dev": "bun run src/main/app.ts",
    "test": "bun test"
  }
}
